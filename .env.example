# Copy to .env and fill in. NEVER commit the filled-in file.
# Both bots run in one process but authenticate as separate Discord users.

# Public bot: lookup commands, added to any guild.
DISCORD_TOKEN=

# Admin bot: management commands, main Stoneworks Discord only.
DISCORD_ADMIN_TOKEN=
